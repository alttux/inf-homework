# Шаблоны для ЕГЭ по информатике

Шаблоны сгенерированы на основе разборов задач из `umschool/`.  
Каждый файл — реальный пример решения (с комментариями, без изменения кода).

---

## Структура

### `num6/` — Задание 6: черепашья графика (Turtle)
| Файл | Описание |
|------|----------|
| `template1.py` | Рисование фигуры без подсчёта точек (просто вывод) |
| `template2.py` | Закрашенный шестиугольник + подсчёт точек внутри через `find_overlapping` |
| `template3.py` | Закрашенный треугольник (111 итераций, угол 120°) + подсчёт |
| `template4.py` | Ломаная фигура (угол 45°) + подсчёт точек внутри |
| `template5.py` | Прямоугольник + подсчёт через `!= ()` |

### `num7/` — Задание 7: кодирование информации (объём изображений)
| Файл | Описание |
|------|----------|
| `template1.py` | Перебор числа кадров k при заданном разрешении и времени |
| `template2.py` | Перебор числа бит на пиксель i, вывод количества цветов (2^i) |

### `num9/` — Задание 9: работа с массивами
| Файл | Описание |
|------|----------|
| `template1.py` | List comprehension: элементы, равные соседнему |
| `template2.py` | List comprehension с условным выражением по формуле |
| `template3.py` | Рекуррентное заполнение массива (разные формулы для чётных/нечётных) |

### `num14/` — Задание 14: системы счисления
| Файл | Описание |
|------|----------|
| `template.py` | Функция перевода числа в произвольное основание |
| `template2.py` | Подсчёт цифр с нужным остатком через цикл `while s > 0` |
| `template3.py` | Поиск x, при котором количество нужных цифр в основании == N |
| `template4.py` | Алфавит с кириллицей для оснований > 36 |
| `template_base_conv.py` | Функция `n_to_x(n, x)` — перевод в основание x |
| `template_while_count.py` | Подсчёт цифр по условию через `while` и `s % base` |
| `template_digit_count.py` | Подсчёт через `bin()`, `oct()`, `hex()` и `.count()` |
| `template_find_x.py` | Перебор x для поиска нужного количества нулей/цифр |
| `template_unknown_digit.py` | Поиск неизвестной цифры в записи числа (основание > 10) |
| `template_bit_manip.py` | Инвертирование битов через `.replace()`, реверс строки |
| `template_reverse_bin.py` | Поиск n, чья развёрнутая двоичная запись равна цели |

### `num15/` — Задание 15: логические выражения
| Файл | Описание |
|------|----------|
| `template.py` | Перебор A: условие через `all()` с логическим выражением |
| `1.py` | Вариант с вспомогательной функцией DEL и подсчётом счётчика |

### `num16/` — Задание 16: рекурсия
| Файл | Описание |
|------|----------|
| `1.py` | Рекурсивная функция, поиск n с заданным значением F(n) |
| `2.py` | Рекурсия с тремя ветками (F(n-1) + F(n-2) + F(n-3)) |
| `3.py` | Рекурсия с `lru_cache` и `setrecursionlimit` для больших n |

### `num17/` — Задание 17: обработка последовательности чисел из файла
| Файл | Описание |
|------|----------|
| `template1.py` | Перебор пар через `range(len-1)`: условие на пару, результат — len + max |
| `template2.py` | Перебор пар через `zip(nums, nums[1:])` с предвычисленным значением |
| `template3.py` | Пары с предвычисленным минимумом среди делящихся на k |
| `template4.py` | Пары: ровно один из двух делится, + условие на среднее чётных |
| `template5.py` | Тройки через `zip(nums, nums[1:], nums[2:])` с условием на количество цифр |

### `num23/` — Задание 23: словари
| Файл | Описание |
|------|----------|
| `template1.py` | Частота слов: `max(dict, key=dict.get)` |
| `template2.py` | Частота элементов: `min` и `max` по значению словаря |
| `template3.py` | Накопление суммы и счётчика, потом вычисление среднего |
| `template4.py` | Условное присваивание значений при чтении, сортировка ключей по значению |
| `template5.py` | Словарь-mapping буква→номер через `enumerate`, суммирование |

### `num24/` — Задание 24: обработка таблиц (два поля, группировка)
| Файл | Описание |
|------|----------|
| `template1.py` | Сортировка строк, поиск пар с одинаковым id и разницей второго поля == 3 |
| `template2.py` | Поиск максимального промежутка между соседними значениями в группе |
| `template3.py` | Частота символьных пар, словарь через `fromkeys`, сортировка по значению |

### `num25/` — Задание 25: числа с заданными свойствами (срезы)
| Файл | Описание |
|------|----------|
| `template1.py` | Перебор кратных делителю, маска через срезы строки (начало + конец) |
| `template2.py` | Маска с подсчётом количества подходящих чисел |
| `template3.py` | Два подхода: срезы строки и `fnmatch` (glob-паттерн) |
| `template4.py` | Однострочник: list comprehension с условием через срез |

### `num26/` — Задание 26: сортировка и отбор (поступление/конкурс)
| Файл | Описание |
|------|----------|
| `template1.py` | Сумма баллов как первый элемент списка → автосортировка |
| `template2.py` | Распаковка со `*`, сортировка по двум ключам через lambda-кортеж |
| `template3.py` | Сортировка по отношению h/p, вторичная сортировка, сумма |
| `template4.py` | Lambda с кортежем из нескольких полей для сортировки |

### `num_ip/` — Задания 10–11: IP-адреса и подсети
| Файл | Описание |
|------|----------|
| `template1.py` | Перебор сети, подсчёт адресов: условие на количество '1' в двоичной записи |
| `template2.py` | Деление адреса пополам: `b[:16]` и `b[16:]` — сравнение частей |
| `template3.py` | Доступ по отрицательному индексу: `net[-2]` — предпоследний адрес |
| `template4.py` | Порядковый номер IP в сети: разность числовых значений адресов |
| `template5.py` | Перебор значения байта A в маске, проверка `all()` по сети |
| `template6.py` | Поиск наибольшего префикса для двух IP в одной сети |
| `template7.py` | Позиция IP в `list(net.hosts())` (нумерация с 1) |
| `template8.py` | Маска как битовый паттерн `'1'*n + '0'*(8-n)`, `all()` по сети |

### `num_lambda/` — Lambda, map, filter, reduce
| Файл | Описание |
|------|----------|
| `template_filter.py` | `filter(lambda, iterable)` — оставляет элементы по условию |
| `template_reduce.py` | `reduce(lambda, iterable)` — произведение всех элементов |
| `template_map1.py` | `map(lambda, iterable)` — среднеквадратическое отклонение |
| `template_map2.py` | `map()` с индексом для подсчёта возрастаний в последовательности |
| `template_median.py` | Lambda-функция медианы, разбивка данных пополам |

### `num_map/` — map() при работе с файлами
| Файл | Описание |
|------|----------|
| `template_map_file.py` | Пользовательская функция + `map()` на каждую строку файла |
| `template_map_sort.py` | `sorted(map(int, ...))` на строку, проверка условия на тройку |

### `14.py` — Задание 12: системы счисления (перебор оснований)
Поиск основания p и цифр в нём через перебор нескольких вложенных циклов.
